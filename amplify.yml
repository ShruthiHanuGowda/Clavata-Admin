version: 1

frontend:
  phases:
    preBuild:
      commands:
        - yarn install --immutable

    build:
      commands:
        - yarn build

  artifacts:
    baseDirectory: dist
    files:
      - '**/*'

  cache:
    paths:
      - node_modules/**/*
      - .yarn/cache/**/*